# Plugin examples
You can find a fully compliant plugin under `sample_plugins/`.  

The other example plugins are `.html` files so they are not fully compatible with the plugin system. However, for the sake of prototyping, the ability to import `.html` files as plugins is still allowed.  

Check `/SPEC.md` for more details on the plugin system.